# Traefik stack environment variables
# Copy to .env and fill in values before running docker compose up -d

# Your domain (e.g. sonicweb.dedicated.co.za)
DOMAIN=

# Email for Let's Encrypt certificate notifications
ACME_EMAIL=

# Traefik dashboard BasicAuth — format: "admin:<bcrypt-hash>"
# Generate with: htpasswd -nB admin
# Or use bootstrap.py which generates this automatically
TRAEFIK_ADMIN_AUTH=

# Timezone
TZ=Africa/Johannesburg
